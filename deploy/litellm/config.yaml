# LiteLLM owns provider credentials and the model aliases exposed to TraderX.
# Add only models that are approved for advisory market research. Example:
#
# model_list:
#   - model_name: openrouter/google/gemini-2.5-pro
#     litellm_params:
#       model: openrouter/google/gemini-2.5-pro
#       api_key: os.environ/OPENROUTER_API_KEY
model_list: []

litellm_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
