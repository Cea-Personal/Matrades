# Model aliases and upstream provider credentials are administered in TraderX →
# Integrations. This bootstrap file deliberately carries no provider keys.
model_list: []

litellm_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  database_url: os.environ/DATABASE_URL
