# Railway configuration for the MT5/Wine runtime service.
# This makes Railway use the custom Dockerfile instead of Railpack's
# automatic start-command detection.

[build]
builder = "DOCKERFILE"
dockerfilePath = "wine.Dockerfile"

[deploy]
startCommand = "/app/start.sh"
