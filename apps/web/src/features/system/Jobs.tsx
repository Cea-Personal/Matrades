export function Jobs() {
  return <section aria-labelledby="jobs"><h2 id="jobs">Background jobs</h2><p>Jobs expose durable state and progress and honor cancellation at safe checkpoints.</p></section>;
}
