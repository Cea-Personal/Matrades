export function Journal() {
  return <section aria-labelledby="journal"><h2 id="journal">Trading journal</h2><p>Automatic trade evidence is immutable; annotations are superseding records rather than edits to history.</p></section>;
}
