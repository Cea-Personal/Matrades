export function RecommendationPanel() {
  return <section aria-labelledby="recommendation-detail"><h2 id="recommendation-detail">Manual-trade recommendation</h2><p>Includes entry, stop, sizing, invalidation, freshness, and expiry. TraderX never submits an order.</p></section>;
}
