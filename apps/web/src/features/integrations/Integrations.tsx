export function Integrations() {
  return <section aria-labelledby="integrations"><h2 id="integrations">Integrations</h2><p>Credentials are write-only. Only explicitly allowlisted read and notification capabilities may be enabled.</p></section>;
}
