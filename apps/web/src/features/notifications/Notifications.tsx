export function Notifications() {
  return <section aria-labelledby="notifications"><h2 id="notifications">Notification inbox</h2><p>Critical notifications remain available in the durable web inbox regardless of channel delivery.</p></section>;
}
