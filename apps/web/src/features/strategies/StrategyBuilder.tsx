export function StrategyBuilder() {
  return <section aria-labelledby="strategy-builder"><h2 id="strategy-builder">Strategy definition</h2><p>Define deterministic rules, invalidation, stop, target, expiry, and risk without executable code.</p><button type="button">Save immutable draft</button></section>;
}
